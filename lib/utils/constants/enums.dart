enum ScreenPage {
  home,
  videoListView,
}

enum IconButtonsBarType {
  actionButtons,
  utilButtons,
  actionAndIconButtons,
}

enum ScreenShadeRotate {
  left,
  top,
  right,
  bottom,
}

enum VolumeButtonIcon {
  on,
  off,
}

enum ToolOrProduct {
  tool,
  product,
}
